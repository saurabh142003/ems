{
  "name": "employee-management-system",
  "version": "1.0.0",
  "description": "Full-stack Employee Management System",
  "private": true,
  "scripts": {
    "dev": "npm --prefix backend run dev",
    "start": "node ./backend/dist/server.js",
    "build": "npm --prefix backend install --include=dev && npm --prefix backend run build && npm --prefix frontend install --include=dev && npm --prefix frontend run build"
  }
}
